# 工作计划

> 最后更新：2026-08-22

## 一、当前项目状态

- 重建项目 `Create: Prismatic Shine`（mod_id: `createprism`）已有 v1.1.0 代码基础
- 基于 LoneStarFateZero 修复版，MC 1.21.1 + NeoForge 21.1.152 + Create 6.0.6
- 仓库已作为 git submodule 挂载在 `repo/Create-Crystal-Clear`
- 远端：origin = LiuJewenTT/Create-Crystal-Clear，upstream = LoneStarFateZero/CreatePrism

## 二、本阶段目标

**先编译验证再修复问题**，确保现有代码能正确编译通过，然后修复已发现的问题。

> 注意：后续重建时名为 `Create Crystal Clear`，包名为 `io.liujiewentt.*`，modId 使用原始仓库的，本阶段暂不改。

## 三、任务清单

### 阶段一：编译验证

1. [x] 配置 Java 21 编译环境（详见 `keep_local/init/dev-env.ps1`）
2. [x] 运行 `gradlew compileJava` 验证编译（BUILD SUCCESSFUL，7 个 deprecation 警告）
3. [x] 修复编译错误（无编译错误，仅 deprecation 警告）

### 阶段二：修复已知问题

4. [x] 修复 mixin 包路径错误（`com.adonis.createprism.mixin` → `com.adonis.prism.mixin`）
5. [x] 清理不相关的 AT 条目（FishingHook 相关条目与当前功能无关）
6. [x] 修复 GitHub Actions JDK 版本（17 → 21）
7. [x] 评估 Copper 照明系列缺失是否需要补充（暂不处理，记录为已知差异）
8. [x] 启用创造栏包裹轴/齿轮展示

### 阶段三：记录与归档

9. [x] 将已处理问题记录到 `docs/problems/已处理的问题-2026-08.md`
10. [x] 潜在问题记录到 `docs/potential-problems/潜在问题.md`
11. [x] 创建 `keep_local/init/dev-env.ps1` 环境加载脚本
12. [x] 重新编译验证修改无误（BUILD SUCCESSFUL）

## 四、发现的问题清单

| # | 问题 | 严重度 | 状态 |
|---|------|--------|------|
| 1 | mixin 配置包路径错误 `com.adonis.createprism.mixin` 应为 `com.adonis.prism.mixin` | 中 | ✅ 已修复 |
| 2 | AT 条目暴露 FishingHook 相关方法，与当前功能无关 | 低 | ✅ 已清理 |
| 3 | GitHub Actions 使用 JDK 17，但 build.gradle 要求 Java 21 | 低 | ✅ 已修复 |
| 4 | Copper 照明系列缺失（枚举不含 copper，无铜照明包裹轴/齿轮） | 中 | ⏸️ 暂不处理 |
| 5 | 创造栏中包裹轴/齿轮展示被注释掉 | 低 | ✅ 已修复 |
| 6 | Ponder 场景为空实现 | 低 | 暂不处理 |
| 7 | addLayer 方法已过时（7 个 deprecation 警告） | 低 | ⏸️ 待定 |

## 五、本机开发环境

详见 `keep_local/init/` 下的环境加载脚本（该目录被 git 忽略，不含敏感路径信息）。