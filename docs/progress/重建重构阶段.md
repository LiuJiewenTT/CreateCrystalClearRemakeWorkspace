# 区别报告：重建重构阶段

> 日期：2026-08-23
> 基线：LoneStarFateZero 修复版（commit `0287466`）
> 当前状态：重建重构 + 阶段五 5.1-5.4 完成（资源补齐 + 依赖移除 + 贴图补全），编译通过（BUILD SUCCESSFUL），已分六次提交

---

## 一、命名重构

### 1.1 gradle.properties 变更

| 属性 | 旧值 | 新值 |
|------|------|------|
| mod_id | `createprism` | `crystal_clear` |
| mod_artifact_id | `createprism` | `crystal_clear` |
| mod_name | `Create: Prismatic Shine` | `Create Crystal Clear` |
| mod_version | `1.1.0` | `2.0.0` |
| mod_group_id | `com.adonis.prism` | `io.liujiewentt.crystal_clear` |
| mod_authors | `Adonis` | `LiuJiewenTT` |
| mod_logo | `Prism.png` | `crystal_clear.png` |

### 1.2 Java 包名重构

- 包路径：`com.adonis.prism` → `io.liujiewentt.crystal_clear`（直接使用带下划线形式，与 modId 保持一致）
- 目录移动：`src/main/java/com/adonis/prism/` → `src/main/java/io/liujiewentt/crystal_clear/`
- 所有 26 个 Java 文件的 `package` 声明和 `import` 语句中的 `com.adonis.prism` 全部替换为 `io.liujiewentt.crystal_clear`

### 1.3 主类改名

- 文件名：`CreatePrism.java` → `CrystalClear.java`
- 类名：`CreatePrism` → `CrystalClear`
- `@Mod(CreatePrism.MOD_ID)` → `@Mod(CrystalClear.MOD_ID)`
- `MOD_ID` 常量值：`"createprism"` → `"crystal_clear"`
- `NAME` 常量值：`"Create Prism"` → `"Create Crystal Clear"`
- 所有引用 `CreatePrism.asResource(...)` 的地方改为 `CrystalClear.asResource(...)`

### 1.4 资源目录移动

- `assets/createprism/` → `assets/crystal_clear/`
- `data/createprism/` → `data/crystal_clear/`

### 1.5 文件改名

- `createprism.mixins.json` → `crystal_clear.mixins.json`
- `createprism.png` → `crystal_clear.png`

### 1.6 JSON 内容替换

- 所有 JSON 文件中的 `createprism:` → `crystal_clear:`
- lang 文件中 `block.createprism.` → `block.crystal_clear.`
- lang 文件中 `itemGroup.createprism.` → `itemGroup.crystal_clear.`

### 1.7 mixins.json 修复

`crystal_clear.mixins.json` 的 `package` 字段：`com.adonis.prism.mixin` → `io.liujiewentt.crystal_clear.mixin`

### 1.8 neoforge.mods.toml 修复

删除 `[[mixins]]` 块下多余的 `file="META-INF/accesstransformer.cfg"` 行（AT 已在 `build.gradle` 中通过 `accessTransformers` 正确注册，此行无效且可能导致解析异常）。

### 1.9 文件移动统计

- git 统计：398 个文件被 git 识别为 R100（100% rename，0 insertions / 0 deletions）
- 文件类型分布：java 26→26, json 322→322, png 50→50
- 唯一不匹配为主类改名（`CreatePrism.java` → `CrystalClear.java`），属预期行为
- 7 个原地修改文件（非移动）：`gradle.properties`、4 个标签 JSON（block/item casing、climbable、mineable）、`crystal_clear.mixins.json`、`neoforge.mods.toml`
- 内容修改阶段另有 342 个文件涉及字符串替换（`createprism` → `crystal_clear`、`CreatePrism` → `CrystalClear`）
- 全部文件中**无 `createprism` 或 `com.adonis` 残留**

## 二、功能补全

### 2.1 Copper 照明包覆系列注册

**修改文件**：`CasingTypes.java`

`ILLUMINATION_ENCASED` 枚举新增 `copper` 条目：

```java
// 修改前（缺少 copper）
ILLUMINATION_ENCASED(
    createHolder("andesite", AllBlocks.ANDESITE_CASING),
    createHolder("brass", AllBlocks.BRASS_CASING),
    createHolder("train", AllBlocks.RAILWAY_CASING)
);

// 修改后（补上 copper）
ILLUMINATION_ENCASED(
    createHolder("andesite", AllBlocks.ANDESITE_CASING),
    createHolder("brass", AllBlocks.BRASS_CASING),
    createHolder("copper", AllBlocks.COPPER_CASING),
    createHolder("train", AllBlocks.RAILWAY_CASING)
);
```

**修改文件**：`en_us.json`、`zh_cn.json`

各补充 3 条翻译：
- `copper_illumination_encased_shaft`
- `copper_illumination_encased_cogwheel`
- `copper_illumination_encased_large_cogwheel`

### 2.2 应力值注册

**修改文件**：`GlassBlockBuilders.java`

新增 import：`com.simibubi.create.api.stress.BlockStressValues`

在 4 个 builder 方法中各添加 `.onRegister(block -> BlockStressValues.IMPACTS.register(block, () -> 0))`：

| 方法 | 行号 | 作用 |
|------|------|------|
| `glassEncasedShaft` | 108 | 玻璃包覆轴 |
| `illuminationEncasedShaft` | 142 | 照明包覆轴 |
| `glassEncasedCogwheel` | 176 | 玻璃包覆齿轮 |
| `illuminationEncasedCogwheel` | 234 | 照明包覆齿轮 |

参考来源：原始项目（c0nnor263 版）的 `RegistrateGenHelper.java` 第130行和第201行做了同样的注册。

## 三、未变更项

以下内容保持与 LoneStarFateZero 修复版一致，本次未做改动：

- **空 Mixin**（`AllArmInteractionPointTypesMixin`）：未注册到 mixins.json，代码为空实现
- **空 Ponder**（`CPPonderPlugin`）：已注册但 scenes/tags 为空
- **可疑战利品表**（5个）：`centrifugal_pump.json`、`copper_tap.json`、`fluid_interface.json`、`pipette.json`、`smart_fluid_interface.json` — 保留不删除
- **old_copper 贴图**（6个）：保留不删除
- **Copper 系列贴图缺失**：5 个 copper 贴图仍缺失，运行时显示紫黑方块但不崩溃
- **addLayer 过时 API**（7 个 deprecation 警告）：未迁移，编译通过
- **AccessTransformer**：空文件（验证阶段已清空无关条目）
- **build.gradle**：无需改动（验证阶段已修复 publishing 块和 CI 配置）

## 四、编译验证

```
gradlew build → BUILD SUCCESSFUL
```

- 7 个 `addLayer` deprecation 警告（已知问题，记录在潜在问题 #2）
- 无编译错误

## 五、Git 提交历史

本次重建重构分为三次提交，各司其职：

| 顺序 | Commit | 类型 | 说明 |
|------|--------|------|------|
| 1 | `023c1ac` | refactor | 纯移动：398 文件 R100 rename，0 insertions / 0 deletions |
| 2 | `e1e532e` | refactor | 命名替换：342 文件字符串替换（包名/类名/modId/翻译键/配置） |
| 3 | `2f95375` | feat | 功能补全：CasingTypes copper 枚举 + GlassBlockBuilders 应力值注册 |

commit message 均使用复选框语法标记各项子任务的完成状态。

> **经验**：采用「先 `git mv` 纯移动并提交 → 再编辑内容并提交」的流程，git 能正确识别为 rename（R100），历史清晰可追溯。

---

## 六、阶段五 5.1：Copper 包覆系列资源补齐

### 6.1 语言文件补齐

**修改文件**：`en_us.json`、`zh_cn.json`

各补充 6 条 copper encased 翻译条目：
- `copper_glass_encased_shaft` / `copper_glass_encased_cogwheel` / `copper_glass_encased_large_cogwheel`
- `copper_clear_glass_encased_shaft` / `copper_clear_glass_encased_cogwheel` / `copper_clear_glass_encased_large_cogwheel`

> 注：`copper_illumination_encased_*` 的 3 条翻译已在阶段四（commit `2f95375`）中补充。

### 6.2 标签文件补齐

**修改文件**：`mineable/pickaxe.json`、`mineable/axe.json`

两个标签文件各补充 2 条 copper encased 条目：
- `crystal_clear:copper_glass_encased_shaft`
- `crystal_clear:copper_clear_glass_encased_shaft`

### 6.3 战利品表补齐

**新增文件**：9 个 loot table JSON

| 文件 | 掉落物 |
|------|--------|
| `copper_glass_encased_shaft.json` | `create:shaft` |
| `copper_glass_encased_cogwheel.json` | `create:cogwheel` |
| `copper_glass_encased_large_cogwheel.json` | `create:large_cogwheel` |
| `copper_clear_glass_encased_shaft.json` | `create:shaft` |
| `copper_clear_glass_encased_cogwheel.json` | `create:cogwheel` |
| `copper_clear_glass_encased_large_cogwheel.json` | `create:large_cogwheel` |
| `copper_illumination_encased_shaft.json` | `create:shaft` |
| `copper_illumination_encased_cogwheel.json` | `create:cogwheel` |
| `copper_illumination_encased_large_cogwheel.json` | `create:large_cogwheel` |

格式参考已有的 `andesite_glass_encased_shaft.json` 等，均为 `minecraft:block` 类型、`survives_explosion` 条件、掉落对应 Create 原件。

### 6.4 Git 提交

| 顺序 | Commit | 类型 | 说明 |
|------|--------|------|------|
| 4 | `ee98a1c` | feat | copper encased 系列资源补全（lang + tags + 9 loot table） |

### 6.5 编译验证

```
gradlew build → BUILD SUCCESSFUL in 16s
```

- 无编译错误
- 无新增警告

---

## 七、移除多余构建依赖

### 7.1 问题

`build.gradle` 中有两个 `localImplementation` 依赖，由 Adonis 添加，Java 代码零引用，`neoforge.mods.toml` 未声明依赖关系：
- `nether-depths-upgrade`（炽海生机）— version 3.1.8
- `geckolib`（炽海生机的依赖库）— version QEqpUJ1G

这两个依赖被 `runtimeClasspath` 继承，导致 `runClient` 时加载了炽海生机及其依赖库 GeckoLib，创造背包中多出"炽海生机"类别。原始项目（c0nnor263 版）完全没有这两个依赖。

### 7.2 修复

| 文件 | 修改 |
|------|------|
| `build.gradle` | 删除 `nether-depths-upgrade` 和 `geckolib` 两行 `localImplementation` |
| `gradle.properties` | 删除 `nether_depths_upgrade_version` 和 `geckolib_version` |

### 7.3 Git 提交

| 顺序 | Commit | 类型 | 说明 |
|------|--------|------|------|
| 5 | `102f8fe` | fix | 移除多余运行时依赖 nether-depths-upgrade 和 geckolib |

### 7.4 编译验证

```
gradlew build → BUILD SUCCESSFUL in 24s
```

- 无编译错误
- 无新增警告

### 7.5 战利品表保留

5 个炽海生机相关战利品表（`centrifugal_pump`、`copper_tap`、`fluid_interface`、`pipette`、`smart_fluid_interface`）保留不删除。它们来自 Adonis 原版，在当前项目中无对应方块注册，运行时不会被加载，无实际影响。跟踪在潜在问题 #8。

---

## 八、Copper 系列贴图补全

### 8.1 背景

Copper 材质的包覆轴/齿轮引用了 4 个缺失贴图：`copper_gearbox`、`copper_encased_cogwheel_side`、`large_copper_encased_cogwheel_side`、`copper_encased_cogwheel_side_connected`。Create 6.0.6 jar 中也没有 `copper_gearbox`（只有 `gearbox` 和 `brass_gearbox`），这是原作者 Adonis 添加 copper 扩展时遗留的未完成工作。

### 8.2 方案演进

| 版本 | 方案 | 结果 |
|------|------|------|
| V1 | HSV 色相旋转（brass→copper） | 与原版风格割裂，已撤销 |
| V3 | 直接调色板映射 | 颜色变平，无高光阴影层次，已撤销 |
| V4 | 以 copper_casing 为底图叠加 brass 结构特征（脚本生成） | 结构正确但用户不满意 |
| 最终 | 用户以 copper_casing 为底图手绘 | 满意，已提交 |

关键教训：brass 贴图的高光/阴影是按黄铜色调设计的，颜色替换后层次感丢失。必须以 copper_casing 为底图，保留铜色高光/阴影/铆钉，参考 brass 结构特征叠加。

### 8.3 修改内容

1. **36 个 model JSON 引用路径修改**（commit `7734865`）：`create:block/copper_gearbox` → `crystal_clear:block/copper_gearbox`（models/block/ 27 个 + models/item/ 9 个）
2. **4 个贴图手绘补全**（commit `30a8e7c` + 用户后续提交）：
   - `textures/block/copper_gearbox.png` — 以 copper_casing 为底图，叠加 brass_gearbox 的暗环+中心黑洞结构
   - `textures/block/encased_cogwheels/copper_encased_cogwheel_side.png` — 以 copper_casing 为底图，中间设透明带（齿轮槽）
   - `textures/block/encased_cogwheels/large_copper_encased_cogwheel_side.png` — 铜色框架+灰色面板，水平条纹结构
   - `textures/block/encased_cogwheels/copper_encased_cogwheel_side_connected.png` — 32x32 四象限 CT 精灵表，用于小齿轮侧面连接贴图

### 8.4 贴图缺失排查

完成全量扫描：模型 JSON 层（195 个文件）、blockstates 层（348 个引用）均无断链。确认铜系列缺失的 4 个贴图已全部由用户手绘补全。

### 8.5 新发现的代码隐患

排查过程中发现 2 处代码与手写 JSON 不一致（当前不影响运行，但 datagen 后会出问题）：

- **#9** `getIlluminationSiding()` 和 `getIlluminationCogCTBehaviour()` 引用 `{material}_illumination_encased_cogwheel_side` 贴图，所有系列均不存在
- **#10** `getBacking()` 对 copper 返回 `copper_backing`，贴图不存在，手写 JSON 用 minecraft 贴图规避

### 8.6 Git 提交

| 顺序 | Commit | 类型 | 说明 |
|------|--------|------|------|
| 6 | `7734865` | feat | 铜系列贴图补全（V1 方案，含 36 JSON 修改 + 调色脚本） |
| 7 | `30a8e7c` | — | 用户手绘铜贴图第二版（替换 V1 贴图） |
| 8 | `3b12e4c` | — | 删除调色脚本 |