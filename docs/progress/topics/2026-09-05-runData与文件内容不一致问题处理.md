# 专题：runData 与文件内容不一致问题处理

> 日期：2026-09-05
> 阶段：阶段六（玻璃机壳包覆铜水管）期间
> 状态：全部处理完毕，修复后复验通过（无新问题）

## 一、背景

阶段六开发完成后，需将 runData 生成的资源从 `src/generated/resources/` 同步到 `src/main/resources/`。同步前对两个目录进行逐文件比对，发现 56 个同名文件存在内容差异。经分析，这些差异并非全部是"生成器正确、手写过时"，而是混合了多种性质的问题。

本专题将所有差异分类、记录，作为独立的技术参考资料。

## 二、核心发现：Gradle 编译缓存掩盖了 runData 崩溃

此前 runData 一直"成功"是因为 `compileJava UP-TO-DATE` / `FROM-CACHE`，实际使用的是旧 class 文件。强制 `--rerun-tasks` 重编译后才暴露 C 类和 F 类的模型缺失崩溃。

**结论：后续执行 runData 验证时必须用 `--rerun-tasks` 参数，否则编译缓存会掩盖代码中的实际错误。**

## 三、差异分类总览

排除 pipe 新增的 12 个条目后，299 个同名文件中 56 个有差异，分为 8 类：

| 类别 | 文件数 | 根因简述 | 处理状态 |
|------|--------|----------|----------|
| A 换行符 | 11 | CRLF vs LF | 无所谓，不需处理 |
| B replace + 排序 | 3 | main 手动加了 `"replace": false`；排序改为 generated 顺序（glass → clear_glass → illumination） | 已修复（排序），replace 字段保留 main 手动值 |
| C illumination shaft parent | 8 | 代码引用不存在的 `illumination_encased_shaft/block` | 已修复 |
| D 小齿轮纹理槽 | 12 | gen 用 `1`/`1_2`/`side`，main 用 `backing`/`cogwheel`/`siding` | 已修复 |
| E 大齿轮纹理槽 | 12 | gen 用 `4`，main 用 `large_cogwheel` | 已修复 |
| F 脚手架 parent + top | 6 | 代码引用不存在的 `scaffold/item` + top 纹理不一致 | 已修复 |
| G en_us.json | 1 | datagen 缺 itemGroup/config 翻译条目 | 已修复（`REGISTRATE.addDataGenerator(ProviderType.LANG, ...)`） |
| H pickaxe.json | 1 | main 缺条目 + glassCasing/illuminationCasing 未调 `pickaxeOnly()` | 已修复（代码加 `pickaxeOnly()` + main 同步 gen） |

## 四、各类详情

### A 类：换行符差异（11 个文件）

**文件**：部分 loot table 和 tag JSON 文件。

**差异**：仅 CRLF vs LF 换行符差异，内容完全相同。

**处理**：无所谓，不需要处理。

### B 类：replace 标记 + 数组排序差异（3 个 tag 文件）

**文件**：3 个 tag JSON 文件（`create/tags/block/casing.json`、`create/tags/item/casing.json`、`minecraft/tags/block/climbable.json`）。

**差异**：
1. `replace` 字段：main 中手动加了 `"replace": false`，gen 没有该字段。NeoForge 的 `TagFile.CODEC` 中 `replace` 使用 `optionalFieldOf("replace", false)`，默认值 `false` 时序列化省略该字段，因此 datagen 永远不输出此字段。省略与显式 `false` 运行时行为完全一致，无法通过 datagen 强制输出。
2. 排序：gen 的 casing.json 顺序为 glass → clear_glass → illumination（与 `CPBlocks.java` 注册顺序一致），main 旧版为 clear_glass → glass → illumination。

**处理**：
- `casing.json`（item + block）：排序改为 gen 顺序（glass → clear_glass → illumination），保留 main 手动的 `"replace": false`。
- `climbable.json`：仅 replace 字段差异，无排序差异，保持不动。
- 代码无需修改：`CPBlocks.java` 注册顺序已为 `GLASS_CASINGS` → `CLEAR_GLASS_CASINGS` → `ILLUMINATION_CASINGS`，与 gen 一致。

### C 类：illumination shaft 模型 parent 引用不存在（8 个文件）

**文件**：4 个 block 模型 + 4 个 item 模型（`{casing}_illumination_encased_shaft.json`）。

**差异**：gen 中 parent 指向 `block/illumination_encased_shaft/block`（和 `/item`），但 `src/main/resources` 中不存在 `illumination_encased_shaft/` 目录（只有 `glass_encased_shaft/`）。

**根因**：`GlassBlockBuilders.java` 的 `illuminationEncasedShaft()` 代码引用了 `illumination_encased_shaft/block` 作为父模型，但该模型文件从未创建。此前因编译缓存未真正重编译而未暴露，强制 `--rerun-tasks` 后 runData 崩溃。

**修复**：改代码复用 `glass_encased_shaft/block` 和 `glass_encased_shaft/item`（结构相同，仅 casing 贴图不同）。从 gen 同步 8 个模型到 main（parent 已指向 glass_encased_shaft）。代码中添加注释说明暂无独立模型计划。

**详见**：`docs/problems/topics/已处理问题-2026-09-05-runData与文件内容不一致问题处理.md` 第 C-1 条。

### D 类：小齿轮 item 纹理槽命名（12 个文件）

**文件**：12 个小齿轮 item 模型（`{casing}_{glass/clear_glass/illumination}_encased_cogwheel.json`）。

**差异**：gen 用 `1`/`1_2`/`side` 纹理槽名（Create 原版命名），main 用 `backing`/`cogwheel`/`siding`（本项目自定义命名）。此前 main 手动改过 JSON 但未改代码，导致 gen 仍用旧名。

**根因**：item 模型父模型（`encased_cogwheel/item.json`）的纹理槽命名与 Create 原版不一致。Datagen 代码写入了自定义名称（`backing`/`cogwheel`/`siding`），但 item 父模型此前未更新，导致 gen 产物纹理槽名称不匹配。

**修复**：在上一轮已修改 item 父模型（`encased_cogwheel/item.json`），统一纹理槽命名为 `casing`/`backing`/`cogwheel`/`siding`/`opening`。本轮 runData --rerun-tasks 验证通过，gen 产物与 main 一致。

**详见**：`docs/problems/topics/已处理问题-2026-09-05-runData与文件内容不一致问题处理.md` 第 D-1 条。

### E 类：大齿轮 item 纹理槽命名（12 个文件）

**文件**：12 个大齿轮 item 模型（`{casing}_{glass/clear_glass/illumination}_encased_large_cogwheel.json`）。

**差异**：gen 用 `4` 纹理槽名（Create 原版），main 用 `large_cogwheel`（本项目自定义）。根因同 D 类。

**修复**：已修改 item 父模型（`encased_large_cogwheel/item.json`），统一纹理槽命名。runData 验证通过。

**详见**：`docs/problems/topics/已处理问题-2026-09-05-runData与文件内容不一致问题处理.md` 第 E-1 条。

### F 类：scaffold item 模型 parent 引用不存在 + top 纹理不一致（6 个文件）

**文件**：6 个 scaffolding item 模型。

**差异**：
1. gen 中 parent 指向 `block/scaffold/item`，但 `src/main/resources` 中不存在 `scaffold/item.json`（只有 `scaffold/block.json` 和 `scaffold/block_horizontal.json`）。
2. gen 的 top 纹理用 `getTopTexture(casing)`（返回 `create:block/funnel/{casing}_funnel_frame`），main 手动改为 `{casing}_glass_casing`。

**根因**：`GlassBlockBuilders.java` 的 `glassScaffolding()` 代码引用了 `scaffold/item` 作为 item model parent，但该文件从未创建。同样被编译缓存掩盖，强制重编译后 runData 崩溃。top 纹理方面，代码用原版 Create 的 funnel_frame 纹理（有镂空效果），main 手动改为 glass_casing（封闭玻璃面），两者外观差异明显。

**修复**：
1. parent 改为 `block/scaffold/block`
2. top 纹理改为 `{casing}_glass_casing`（与 main 的手动修正一致）
3. 代码中添加 TODO 注释记录原始纹理和未来设计方向

**详见**：`docs/problems/topics/已处理问题-2026-09-05-runData与文件内容不一致问题处理.md` 第 F-1 条。

### G 类：en_us.json 缺 itemGroup/config 翻译条目（1 个文件）

**文件**：`src/main/resources/assets/crystal_clear/lang/en_us.json`。

**差异**：main 版本包含手工添加的 itemGroup 和 config 相关翻译条目（共 5 条），gen 版本中缺失。

**根因**（修正）：最初判断为"main 手工条目保持不动"，实际是 datagen 代码缺陷。Registrate 只为通过 `REGISTRATE.block()/item()` 注册的方块/物品自动生成 lang 条目，而 `itemGroup.crystal_clear.main`（创造标签页标题）和 `config.crystal_clear.creative.*`（NeoForge 配置屏幕/Configured mod 显示文本）不是通过 Registrate 注册的，因此不会被自动生成。需要在代码中通过 `REGISTRATE.addDataGenerator(ProviderType.LANG, ...)` 手动添加。

**修复**：在 `CrystalClear.java` 的 static 块中添加 `REGISTRATE.addDataGenerator(ProviderType.LANG, ...)`，手动添加 5 条翻译：
- `itemGroup.crystal_clear.main`：创造标签页标题
- `config.crystal_clear.creative`：配置分类标题
- `config.crystal_clear.creative.tooltip`：配置分类提示
- `config.crystal_clear.creative.show_encased_variants`：配置项名称
- `config.crystal_clear.creative.show_encased_variants.tooltip`：配置项说明

此方式参考 Create 原版 `CreateDatagen.addExtraRegistrateData()`，用法一致。修复后 gen 的 en_us.json 包含全部条目，与 main 内容一致（仅 CRLF 差异）。main 的 en_us.json 已从 gen 同步。

### H 类：pickaxe.json 缺失条目（1 个文件）

**文件**：`src/main/resources/data/minecraft/tags/block/mineable/pickaxe.json`。

**差异**：gen 版本有 66 个条目，main 版本原只有 18 个条目。

**根因**（修正）：
1. 28 个非 pipe 条目（shaft/cogwheel/large_cogwheel/scaffolding）是 main 历史遗漏，代码中已调用 `pickaxeOnly()`，datagen 生成正确。
2. 4 个 illumination_casing 条目缺失——`GlassBlockBuilders.illuminationCasing()` 方法未调用 `.transform(pickaxeOnly())`，代码缺陷。
3. 8 个 glass_casing/clear_glass_casing 条目缺失——`glassCasing()` 方法同样未调用 `pickaxeOnly()`，代码缺陷。
4. 12 个 pipe 条目属于阶段六新增，待阶段六一并处理。

**修复**：
- 在 `glassCasing()` 和 `illuminationCasing()` 方法中添加 `.transform(pickaxeOnly())`（与所有其他 builder 一致）
- 从 main 的 pickaxe.json 删除手工添加的 4 条 illumination_casing（让 datagen 自动生成）
- 从 gen 同步 pickaxe.json 到 main，排除 12 条 pipe 条目（阶段六处理）
- 最终 main 的 pickaxe.json 有 54 条（全部非 pipe 条目），gen 有 66 条（含 12 条 pipe）

## 五、纹理由来与设计说明

### 纹理槽命名规则（D/E 类）

| 纹理含义 | block 系列模型 | item 父模型（小齿轮） | item 父模型（大齿轮） |
|----------|---------------|----------------------|----------------------|
| 机壳面 | `casing` | `casing` | `casing` |
| 木盘背面 | `backing` | `backing` | `backing` |
| 齿轮侧面 | `siding` | `siding` | `siding` |
| 齿轮本体 | — (渲染器绘制) | `cogwheel` | `large_cogwheel` |
| 开口面 | `opening` | `opening`（默认值） | `opening`（默认值） |

所有 24 个子 item 模型统一提供 `backing`/`casing`/`opening`/`siding`。

### Blockbench 预览问题

Blockbench "打开父级并应用纹理"功能不生效是 BB 自身 bug。item 父模型保留默认纹理值是合理设计（方便直接打开父模型编辑），游戏内 Minecraft 模型解析子模型覆盖父模型默认值，行为正确。两个 item 父模型维持现状，不做修改。

## 六、修复后复验

### 第一轮复验（B/H 类修复后）

B 类和 H 类修复提交后，重新执行 `runData --rerun-tasks` 生成资源，再次运行对比脚本。

统计：Common 359 / gen-only 38 / main-only 6104 / 差异 14。

14 个差异全部属于已知的 A/B/G/H 类，无新问题。但 G 类（en_us.json）和 H 类（pickaxe.json illumination_casing 缺失）的根因判断有误——实际是代码缺陷而非"手工条目保留"。

### 第二轮修复（G/H 类根因修正）

**G 类修复**：在 `CrystalClear.java` static 块中通过 `REGISTRATE.addDataGenerator(ProviderType.LANG, ...)` 添加 5 条非方块/物品翻译条目（itemGroup + config）。参考 Create 原版 `CreateDatagen.addExtraRegistrateData()` 用法。

**H 类修复**：在 `GlassBlockBuilders.java` 的 `glassCasing()` 和 `illuminationCasing()` 方法中添加 `.transform(pickaxeOnly())`（与所有其他 builder 一致）。从 main 删除手工添加的 illumination_casing 条目。

### 第二轮复验结果

`runData --rerun-tasks` BUILD SUCCESSFUL。对比结果：

统计：Common 359 / gen-only 38 / main-only 6104 / **差异 13**（比第一轮少 1，en_us.json 差异已消除）。

| # | 文件 | 差异类型 | 对应类别 | 处置 |
|---|------|----------|----------|------|
| 1 | `en_ud.json` | gen-only 含此文件，main 无 | 正常 datagen 产物 | 不入 main |
| 2-9 | 8 个 copper loot_table | JSON 解析后完全一致，纯 CRLF | A 类 | 无所谓 |
| 10-11 | `casing.json` ×2 | 仅 replace 字段 | B 类 | 已处理 |
| 12 | `climbable.json` | 仅 replace 字段 | B 类 | 已处理 |
| 13 | `pickaxe.json` | gen 多 12 条 pipe | H 类/阶段六 | 待阶段六处理 |

**结论：G 类和 H 类代码缺陷已修复，en_us.json 和 pickaxe.json 的非 pipe 差异全部消除。剩余 13 个差异均为已知类别（A/B 类 + pipe 待处理），无新问题。**