# 工作计划 - 存档（阶段五：功能迁移与完善）

> 原始日期：2026-08-23 ~ 2026-08-27
> 转存日期：2026-08-28
> 状态：阶段五全部完成

## 一、本阶段目标：功能迁移与完善

将原始项目（c0nnor263 修复版）中的功能特性迁移到 Adonis 重构版基础上，使功能"只多不少"。代码保持 Adonis 风格（NeoForge 单平台），不复制 c0nnor263 的 ARR 代码。疑似多余的功能项写入潜在问题文档。

### 需要迁移的功能

| 功能项 | 来源 | 迁移方式 | 难度 |
|--------|------|----------|------|
| BlockStressValues 应力值注册 | c0nnor263 `RegistrateGenHelper` | 已在阶段四完成 | - |
| 配方 advancements | c0nnor263 自动生成 | 运行 datagen 或不处理（Adonis 用手写资源） | 低 |

### 需要完善的现有功能

| 功能项 | 当前状态 | 完善内容 |
|--------|----------|----------|
| Copper 包覆系列语言文件 | 缺少 encased shaft/cogwheel 条目 | 补充 en_us/zh_cn 翻译 |
| Copper 包覆系列标签 | 缺少 mineable 标签 | 补充 pickaxe/axe 标签 |
| Copper 包覆系列战利品表 | 缺少 loot table | 补充 loot table JSON |
| Copper 系列贴图 | 5 个贴图缺失 | 制作或复用替代贴图（待定） |

### 不在本次处理

- addLayer 过时 API 迁移（待定）
- 空 Mixin / Ponder 场景（暂不处理）
- 创造栏策略（暂不修改）
- Steel 材质（c0nnor263 也只是半成品，无代码注册，暂不迁移）
- 不做任何删除操作（保留所有现有文件）

### 疑似多余项（写入潜在问题，不在工作计划中展开）

- 可疑战利品表（5 个非本模组方块）
- 空 Mixin / 空 Ponder / 空 CPPartialModels
- old_copper 贴图（6 个）
- 额外杂项贴图（8 个 Create 原版贴图副本）
- mineable/axe 标签（与 pickaxe 内容相同，行为合理性待定）
- 大齿轮代码中 blockFolder 与手写 JSON 的 parent 路径不一致（#6 #12 已彻底修复）

## 二、任务清单

#### 5.1 Copper 包覆系列资源补齐

1. [x] 补充 copper encased shaft/cogwheel/large_cogwheel 的语言文件条目（en_us + zh_cn）
2. [x] 补充 copper encased 系列的 mineable 标签（pickaxe + axe）
3. [x] 补充 copper encased 系列的 loot table JSON（9 个文件）

#### 5.2 验证与归档

4. [x] 编译验证（`gradlew build` 通过）
5. [x] 更新文档（progress 报告、已处理问题、潜在问题）
6. [x] 提示用户执行 AIGC 去水印

#### 5.3 移除多余构建依赖

7. [x] 移除 `build.gradle` 中 `nether-depths-upgrade`（炽海生机）和 `geckolib` 依赖
8. [x] 清理 `gradle.properties` 中对应版本号
9. [x] 编译验证
10. [x] 更新文档

#### 5.4 Copper 系列贴图补全

11. [x] 修改 36 个 model JSON 引用路径（`create:block/copper_gearbox` → `crystal_clear:block/copper_gearbox`）
12. [x] 用户手绘补全 3 个贴图（copper_gearbox、copper_encased_cogwheel_side、large_copper_encased_cogwheel_side）
13. [x] 补全 `copper_encased_cogwheel_side_connected.png`（32x32 CT 精灵表，用户手绘）
14. [x] 更新文档（潜在问题 #5 状态更新、新增 #9 illumination 路径不一致、新增 #10 copper_backing 不一致、已处理问题 #11）

#### 5.5 代码-资源一致性修复（潜在问题 #6 #9）

15. [x] 修复 #6：`GlassBlockBuilders.java` 4 处 `blockFolder` 分叉代码统一为 `"encased_cogwheel"`，注释说明原因
16. [x] 修复 #9：创建 12 个 illumination 专用 siding 贴图（复制自普通版），修改 32 个 illumination 模型 JSON 引用为 illumination 专用路径，使代码与 JSON 完全一致
17. [x] 编译验证（BUILD SUCCESSFUL）
18. [x] 更新文档（潜在问题 #6 #9 标记已修复、已处理问题新增 #12 #13、进度报告追加第九节）

#### 5.6 配置功能（创造模式标签页显示控制）

19. [x] 创建 `CPConfig.java` 配置类（CLIENT 类型，`show_encased_variants` 默认 false）
20. [x] 修改 `CrystalClear.java` 注册配置
21. [x] 修改 `CPCreativeTab.java` 包覆轴/齿轮用配置控制显示
22. [x] 添加配置项翻译（en_us + zh_cn）
23. [x] 编译验证（BUILD SUCCESSFUL）
24. [x] 更新文档（已处理问题 #14、进度报告追加第十节）
25. [x] Better ModList 全版本验证不可用（javap 反编译确认 1.0.2~21.1.1 均有 @SubscribeEvent bug）
26. [x] 集成 Configured mod（MrCrayfish）作为开发环境运行时配置 GUI（`localRuntime`，不打包到发布 jar）：下载 jar 到 libs/、build.gradle flatDir 引用
27. [x] JEI 版本升级 19.21.0.247 → 19.24.0.317（满足 Configured 最低版本要求）
28. [x] 编译验证（BUILD SUCCESSFUL）
29. [x] runClient 验证（开发环境）：游戏正常启动，Configured 2.6.1 + JEI 19.24.0.317 全部加载成功，配置界面可正常编辑并保存
30. [x] 更新文档（已处理问题 #15、进度报告追加第十一节）
31. [x] 适配 NeoForge 内置 ConfigurationScreen 方案：注释掉 Configured `localRuntime` 依赖，注册 `IConfigScreenFactory` 扩展点（两种方案并行存在）
32. [x] runClient 验证内置界面注册成功，发现 TranslationChecker 报告 4 个翻译键缺失
33. [x] 反编译 TranslationChecker 和 ConfigurationScreen 字节码，确认翻译键生成规则
34. [x] 决定不添加 4 个内置界面翻译键：TranslationChecker 有 fallback 机制，默认效果已足够好，属于可选个性化扩展
35. [x] 更新知识文档（NeoForge配置GUI翻译机制.md 补充内置界面翻译键约定，标注本项目不添加）

#### 5.7 大齿轮独立模型修复（#12 延伸）

36. [x] 引入 Create 6.0.6 大齿轮专用模型目录 `encased_large_cogwheel/`（5 个 JSON，用户引入并提交 commit `e94d013`）
37. [x] 修改 60 个大齿轮模型 JSON 的 parent 引用（48 block + 12 item）：`encased_cogwheel/*` → `encased_large_cogwheel/*`
38. [x] 编译验证（BUILD SUCCESSFUL）
39. [x] runClient 验证（用户确认创造背包中大齿轮显示正常）
40. [x] 更新文档（已处理问题 #12 延伸修复、进度报告追加第十三节、future 文档措辞更新）
41. [x] 修复大齿轮父模型纹理变量不匹配：`encased_large_cogwheel/` 5 个父模型 JSON 的 `#side`/`#1`/`#4` 统一为 `#siding`/`#backing`/`#opening`（item.json 保留 `#4`），移除 block 系列默认贴图值
42. [x] 恢复 `GlassBlockBuilders.java` 4 处 `blockFolder` 的 `large` 分叉
43. [x] 编译验证（BUILD SUCCESSFUL）
44. [x] 更新文档（潜在问题 #6 状态、已处理问题 #12 补充第二步、进度报告追加第十四节）
45. [x] 修正大齿轮父模型上下壳 UV（上壳避开 y8-9 透明带、下壳选取完整 6 行区域）
46. [x] 上下壳侧面 UV 横向拉满（用户改默认侧面纹理为 16×16 大齿轮版本后，UV x 从 8 → 0）
47. [x] 修复安山玻璃照明大齿轮变体数据错误：`andesite_illumination_encased_large_cogwheel{,_top,_bottom,_top_bottom}.json` 的 siding 引用普通版贴图 + top/bottom parent 交叉错位（#13 修复时的遗漏）
48. [x] 全面复查：12 个大齿轮 block 变体 siding 均用 large 版、parent 均指向 encased_large_cogwheel/，JSON 有效
49. [x] 编译验证（BUILD SUCCESSFUL）
50. [x] runClient 验证（用户确认上下壳侧面正常、安山照明大齿轮变体正常）
51. [x] 更新文档（已处理问题 #12 追加第三步、进度报告追加第十五节）

#### 5.8 copper_backing 贴图补全与引用修正（潜在问题 #10）

52. [x] 复制原版 `copper_block.png`（16×16）为 `crystal_clear:block/copper_backing` 贴图
53. [x] 修改 30 个 copper 系列模型 JSON 的 backing 引用：`minecraft:block/stripped_dark_oak_log_top` → `crystal_clear:block/copper_backing`（brass 不受影响）
54. [x] 编译验证（BUILD SUCCESSFUL）
55. [x] 更新文档（潜在问题 #10 标记已修复、已处理问题新增 #17）