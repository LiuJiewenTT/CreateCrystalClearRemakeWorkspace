# 工作计划 - 存档（阶段四：重建重构）

> 原始日期：2026-08-22 ~ 2026-08-23
> 转存日期：2026-08-28
> 状态：阶段四全部完成

## 一、本阶段目标：重建重构

将 LoneStarFateZero 修复版（CreatePrism）完全重建为 Create Crystal Clear，包括命名重构和功能补全。

- 基线：LoneStarFateZero 修复版（commit `0287466`）
- 编译通过，分三次提交

## 二、任务清单

### 4.1 命名重构

1. [x] 修改 `gradle.properties`（mod_id、mod_name、mod_group_id、mod_artifact_id、mod_authors、mod_logo、mod_version）
2. [x] Java 源码内容替换：`com.adonis.prism` → `io.liujiewentt.crystal_clear`、`CreatePrism` → `CrystalClear`
3. [x] Java 目录移动：`com/adonis/prism/` → `io/liujiewentt/crystal_clear/`
4. [x] 资源目录移动：`assets/createprism/` → `assets/crystal_clear/`、`data/createprism/` → `data/crystal_clear/`
5. [x] 文件改名：`createprism.mixins.json` → `crystal_clear.mixins.json`、`createprism.png` → `crystal_clear.png`
6. [x] mixins.json 内容更新（package、refmap 字段）
7. [x] 全局替换 JSON 文件中 `createprism:` → `crystal_clear:`
8. [x] 全局替换 lang 文件中 `block.createprism.` → `block.crystal_clear.`、`itemGroup.createprism.` → `itemGroup.crystal_clear.`
9. [x] 修复 neoforge.mods.toml 模板中 `[[mixins]]` 块多余的 `file=` 行

### 4.2 功能补全

10. [x] 补注册 Copper 照明包覆系列（CasingTypes 枚举 + lang + 标签）
11. [x] 补注册应力值为 0

### 4.3 验证与归档

12. [x] 编译验证（`gradlew build` 通过）
13. [x] 更新文档：已处理问题记录到 `docs/problems/`，待考虑问题记录到 `docs/potential_problems/`

## 三、提交记录

- `023c1ac` — 纯移动（398 文件 R100）
- `e1e532e` — 命名替换（342 文件字符串替换）
- `2f95375` — 功能补全（CasingTypes + GlassBlockBuilders）

> 详见 [progress/重建重构阶段.md](progress/重建重构阶段.md)