# 未来参考选用事项

> 记录不一定应当要改、未来可能会考虑改的事项。仅作参考，不代表一定会执行。

---

## 1. 小齿轮包覆箱模型替换

**日期**：2026-08-26

**来源**：commit `e94d013`（引入 Create 大包覆齿轮箱模型时的提交备注）

**背景**：

在引入 Create 6.0.6 自带的大包覆齿轮箱模型（`encased_large_cogwheel/` 目录）时，同时对小齿轮包覆箱的模型也做了略微更改，但视觉上没看出明显区别。

**事项**：

后续可以考虑也替换掉小齿轮包覆箱的模型，使用 Create 自带的 `encased_cogwheel/` 系列模型（如果有的话），与大齿轮保持一致的来源风格。

**当前状态**：

小齿轮包覆箱仍使用原有模型。如需替换，需对比 Create 原版 `encased_cogwheel/` 模型与当前模型的差异，确认替换后视觉效果是否更优。